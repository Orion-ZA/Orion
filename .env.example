# Copy this file to .env.local (git-ignored) and fill in the values
# React environment variables must be prefixed with REACT_APP_

REACT_APP_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
# Firebase values (if not already provided elsewhere)
# REACT_APP_FIREBASE_API_KEY=your_key
# REACT_APP_FIREBASE_AUTH_DOMAIN=your_domain.firebaseapp.com
# ... add other firebase config keys as needed
